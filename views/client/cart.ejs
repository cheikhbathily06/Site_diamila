<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/luxury-design.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/responsive.css">
  <link rel="stylesheet" href="/css/golden-design.css">
</head>
<body>
  <header>
    <div class="header-container">
      <a href="/" class="logo">
        <% if (logo) { %><img src="<%= logo %>" alt="Logo"><% } %>
        <h1>Denisia Bijoux</h1>
      </a>
      <nav>
        <ul class="nav-menu">
          <li><a href="/">Accueil</a></li>
          <li><a href="/boutique">Boutique</a></li>
          <li><a href="/a-propos">À propos</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </nav>
      <div class="header-actions">
        <a href="/panier" class="cart-icon">
          <i class="fas fa-shopping-cart"></i>
          <% if (cart && cart.length > 0) { %>
            <span class="cart-count"><%= cart.length %></span>
          <% } %>
        </a>
      </div>
    </div>
  </header>

  <main>
    <div class="cart-container container">
      <h1 class="section-title">Mon Panier</h1>

      <% if (!cart || cart.length === 0) { %>
        <div class="empty-state">
          <div class="empty-state-icon">
            <i class="fas fa-shopping-cart"></i>
          </div>
          <h2>Votre panier est vide</h2>
          <p>Découvrez nos magnifiques bijoux en acier inoxydable</p>
          <a href="/boutique" class="btn-primary">Continuer mes achats</a>
        </div>
      <% } else { %>
        <div class="cart-items">
          <% 
          // MODIFICATION 1: Calcul du total dans le panier
          let cartSubtotal = 0;
          cart.forEach((item, index) => { 
            const itemTotal = item.price * item.quantity;
            cartSubtotal += itemTotal;
          %>
            <div class="cart-item" data-price="<%= item.price %>">
              <% if (item.image) { %>
                <img src="<%= item.image %>" alt="<%= item.name %>" class="cart-item-image">
              <% } else { %>
                <div class="cart-item-image placeholder">
                  <i class="fas fa-gem"></i>
                </div>
              <% } %>
              
              <div class="cart-item-info">
                <h3><%= item.name %></h3>
                <p class="cart-item-price"><%= item.price.toLocaleString('fr-FR') %> FCFA</p>
                <% if (item.personalizationText) { %>
                  <p class="personalization-text">
                    <i class="fas fa-pen"></i> Personnalisé: "<%= item.personalizationText %>"
                  </p>
                <% } %>
              </div>

              <div class="cart-item-actions">
                <div class="quantity-control">
                  <button onclick="updateCartQuantity(<%= index %>, <%= item.quantity - 1 %>)" <%= item.quantity <= 1 ? 'disabled' : '' %>>
                    <i class="fas fa-minus"></i>
                  </button>
                  <input type="number" class="quantity-input" value="<%= item.quantity %>" min="1" readonly>
                  <button onclick="updateCartQuantity(<%= index %>, <%= item.quantity + 1 %>)">
                    <i class="fas fa-plus"></i>
                  </button>
                </div>
                <p class="item-subtotal"><%= itemTotal.toLocaleString('fr-FR') %> FCFA</p>
                <button class="btn-remove" onclick="removeFromCart(<%= index %>)">
                  <i class="fas fa-trash"></i> Supprimer
                </button>
              </div>
            </div>
          <% }) %>
        </div>

        <!-- MODIFICATION 1: Affichage du total dans le panier -->
        <div class="cart-summary">
          <h3>Récapitulatif</h3>
          <div class="summary-row">
            <span>Sous-total (<%= cart.length %> article<%= cart.length > 1 ? 's' : '' %>):</span>
            <span class="summary-amount"><%= cartSubtotal.toLocaleString('fr-FR') %> FCFA</span>
          </div>
          <div class="summary-note">
            <i class="fas fa-info-circle"></i>
            Les frais de livraison seront calculés à l'étape suivante
          </div>
          <div class="summary-actions">
            <a href="/boutique" class="btn-secondary">
              <i class="fas fa-arrow-left"></i> Continuer mes achats
            </a>
            <a href="/paiement" class="btn-primary">
              <i class="fas fa-arrow-right"></i> Passer la commande
            </a>
          </div>
        </div>
      <% } %>
    </div>
  </main>

  <footer>
    <div class="footer-content">
      <div class="footer-section">
        <h3>Denisia Bijoux</h3>
        <p>Bijoux en acier inoxydable de qualité premium</p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; <%= new Date().getFullYear() %> Denisia Bijoux. Tous droits réservés.</p>
    </div>
  </footer>

  <script src="/js/main.js"></script>
  <script src="/js/mobile.js"></script>
</body>
</html>
